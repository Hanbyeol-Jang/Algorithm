SELECT      DATE_FORMAT(DATETIME,   '%H')   AS  HOUR
        ,   COUNT(DATE_FORMAT(DATETIME,   '%H'))
FROM        ANIMAL_OUTS

WHERE       DATE_FORMAT(DATETIME,   '%H')   >= 9 AND
            DATE_FORMAT(DATETIME,   '%H')   <= 19

GROUP BY    HOUR
ORDER BY    HOUR